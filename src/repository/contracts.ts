export function contractAddress(contract: string, chain: Chain | undefined, ) {
    if (contract == "window") {
        switch (network) {
            case "ethereum":
                console.log("I own a lizard");
                break;
            default:
                return "0xbC873913990da5673D739be7a2C15AEbA4e651b5"
        }
    }

}